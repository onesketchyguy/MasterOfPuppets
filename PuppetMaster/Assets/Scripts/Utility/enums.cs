public enum PhysicalState { Fine, Stunned, Crippled, Blind, OnFire, Frozen, Sleeping }

public enum DetectionStates { UnAware, Aware, InSight, Suspicious }

public enum GameState { Playing, Paused, InInventory, InChat }

public enum AttackType { Physical, Acid, Alien, Fire, Ice, Fungus }

public enum EquipmentTier { none, Tier_1, Tier_2, Tier_3, Tier_4, Tier_5 }

public enum EquipmentSlot { none, head, body, feet, legs, cloak, shoulders, hands, weapon }

public enum MovementState { Stationary, Wandering, Targeting, Following, Working }

public enum CombatHostility { none, AttackOnAttacked, AttackOnSight };

public enum HostilityLevel { Neuteral, Friendly, Hostile };

public enum BodyType { Human, Machanical, Alien }

public enum ItemType { shiny, simple, complex, equipment, weapon, potion, ingredient, food, water }